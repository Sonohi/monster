function simConfig = monsterSetup(Param)
% monsterSetup - performs all the task related to setting up a simulation 
%
% Syntax: simConfig = monsterSetup(Param)
%
% monsterSetup is called from the main simulation file and returns a simulation configuration object

	
	simConfig = MonsterConfig(Param);

	
end