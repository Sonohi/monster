Install sphinx for compiling docs

`pip install -r requirements.txt`

For compiling the docs run

`sphinx-build -b html source/ build/`

See https://pypi.org/project/sphinxcontrib-matlabdomain/ for syntax