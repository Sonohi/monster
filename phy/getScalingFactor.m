function [k] = getScalingFactor(Station, User)

%   GET SCALING FACTOR  is used to get the rx waveform scaling for an interferer	 
%
%   Function fingerprint
%   Station        		->  the interfering base station
%   User        			->  the served user
%
%   k	      					->  scaling factor
%

	% TODO remove placeholder
	k = 1;
end
